
<div class="row">
    {{#each datosBD}}
    <div id="Cuad" class="col-md-3 ">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title d-flex justify-content-between align-items-center">
                    {{titulo}} <a href="#" ><i class="fas fa-pencil-alt"></i></a>
                </h4>
                <p>{{descripcion}}</p>
                <a href="#" class="btn btn-danger btn-block btn-sm">delete</a>

            </div>
        </div>
    </div>
    {{else}}

    {{/each}}
</div>